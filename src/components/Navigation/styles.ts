import { AccountIcon, BasketIcon, FavoritesIcon } from "assets/icons";
import styled from "styled-components";

export const StyledNavigation = styled.div`
  display: flex;
`;

export const StyledAccountIcon = styled(AccountIcon)`
  width: 56px;
  height: 56px;
`;

export const StyledBasketIcon = styled(BasketIcon)`
  width: 56px;
  height: 56px;
`;

export const StyledFavoritesIcon = styled(FavoritesIcon)`
  width: 56px;
  height: 56px;
`;
